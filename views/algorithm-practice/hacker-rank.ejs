<!DOCTYPE html>
<html lang = "en">

    <% include ../partials/page/head.ejs %>

    <body class = "fixed-header">
        <% include ../partials/page/header.ejs %>

        <% include ../partials/page/search.ejs %>
        <div id = "wrapper">
            <section class = "elements">
                <div class = "container">
                    <h3>Test Page</h3>

                    <p>
                        Follow up for "Remove Duplicates":<br>
                        What if duplicates are allowed at most twice?<br>
                    </p>

                    <p>
                        For example,
                        Given sorted array <code>nums = [1,1,1,2,2,3]</code>,
                    </p>

                    <p>
                        Your function should return <code>length = 5</code>,
                        with the first
                        five elements of <code>nums</code> being <code>1, 1, 2,
                            2</code> and <code>3</code>.
                        It doesn't matter what you leave beyond the new length.
                        When $a \ne 0$, there are two solutions to \(ax^2 + bx +
                        c = 0\) and they are
                        $$x = {-b \pm \sqrt{b^2-4ac} \over 2a}.$$

                    <p>When `a != 0`, there are two solutions to `ax^2 + bx + c
                        = 0` and
                        they are
                    </p>

                    <p style = "text-align:center">
                        `x = (-b +- sqrt(b^2-4ac))/(2a) .`
                    </p>
                    </p>

                    <pre class = "line-numbers language-java">
                        <code class = "language-java">
                            public int removeDuplicates(int[] nums) {
                            int i = 0;
                            for (int n : nums)
                            if (i < 2 || n > nums[i-2])
                            nums[i++] = n;
                            return i;
                            }
                        </code>
                    </pre>

                </div>
            </section>

        </div>
        <!-- /#wrapper -->

        <% include ../partials/page/footer.ejs %>

        <% include ../partials/page/script.ejs %>

    </body>
</html>